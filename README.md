# flet-sample-forks
gather Flet-Samples

to delete remote repo
how?. 
"""
git rm -r --cached .
git add .
git commit -m "gathering"
git push
"""

#how to   <br>
original git repo address to find / show?
